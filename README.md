# Test Python Repo

This is a test Python repository.

## Setup

1. Clone the repository
2. Install the package: `pip install -e .`

## Usage

```python
from src.main import hello_world

hello_world()
```

## Running Tests

Run `python -m unittest discover tests` from the project root directory.
